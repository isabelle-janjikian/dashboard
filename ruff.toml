line-length = 110
target-version = "py311"

[lint]
select = ["E", "F", "I", "W"]
# E501 (ligne trop longue) désactivé : generate_dashboard.py embarque de longs
# templates HTML/JS/SVG en chaînes Python, où couper les lignes nuirait à la lisibilité.
ignore = ["E501"]
